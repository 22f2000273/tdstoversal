# Core requirements
fastapi==0.68.0
uvicorn==0.15.0
python-dotenv==0.19.0
requests==2.26.0
numpy==1.21.0
aiohttp==3.8.1
sqlite3==  # (normally comes with Python, no version needed)

# Development/Server
pytest==7.0.0
httpx==0.23.0

# Optional (only include if actually used)
# pandas==1.3.0
# scikit-learn==1.0.0